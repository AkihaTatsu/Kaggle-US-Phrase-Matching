1、模型说明
	模型不止是DeBERTa模型，也可以直接套用ELECTRA模型以及XLNet模型（小样本测试中前者较不稳定，后者很不稳定）
	合理外推，理论上可以套用几乎所有huggingface上的模型？？？，有相中的可以加上去跑跑
	模型训练函数那里有个存储模型的过程trainer.save_model()，path参数是save_dir，你看看能不能直接用什么的
2、关于验证集
	lzr他们经验，可以训练比验证集9：1，实际提交的时候也这么做（lzr：百分之九十的训练集和所有验证集差不多），感觉这个时候随机种子的作用就比较显著了，记得划分的时候用随机种子
3、关于结果的组合
	不同模型间、相同模型不同层次间的结合可以不止简单线性的，可以考虑之前机器学习的集成学习，如下一层模型学习上层模型的残差等等。